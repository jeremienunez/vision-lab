# Presentation Notes

Delivery surfaces planned for PerceptionLab:

- Rust HTTP API.
- OpenAPI documentation.
- Curl examples for the demo flow.
- Optional CLI after the API-first MVP.

Dashboard work is intentionally out of the MVP until the API, worker, registry, inference, and export flow is credible.
