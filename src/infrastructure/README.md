# Infrastructure Notes

Infrastructure adapters planned for PerceptionLab:

- PostgreSQL repositories.
- Object storage or local filesystem storage.
- Queue producer and consumer.
- Artifact registry.
- PyTorch worker contract.
- ONNX export adapter.

Concrete adapters must remain replaceable through application ports.
