# QA Feature Fixtures

Expected fixture groups:

- `images/`: `cup.jpg`, `book.jpg`, `phone.jpg`, `invalid.txt`, `oversized.jpg`, `corrupted.jpg`.
- `annotations/`: `desk_objects_yolo.zip`.
- `payloads/`: `create_dataset.json`, `create_training_job.json`.

Binary fixtures will be added when the API and worker test profile exist.
