# PyTorch Training Worker

Planned responsibility:

- Consume queued training and export jobs.
- Materialize immutable dataset versions for PyTorch.
- Run fine-tuning or a controlled mock training mode for local demo speed.
- Write metrics and job status updates.
- Store model artifacts and exports.
- Run inference and generate overlays when assigned to the worker.
