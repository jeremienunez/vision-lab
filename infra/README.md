# Infrastructure

Planned MVP local stack:

- Rust API service.
- Python/PyTorch worker.
- PostgreSQL.
- Queue.
- MinIO or local filesystem storage adapter.
- Docker Compose.

The MVP must remain runnable locally and understandable by a technical recruiter in under ten minutes.
