import { Database } from 'lucide-react';
import { EmptyState } from '../../components/EmptyState.jsx';

export function DatasetsPage() {
  return <EmptyState icon={Database} label="Datasets view — migrating" />;
}
