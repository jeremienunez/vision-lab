import { Camera } from 'lucide-react';
import { EmptyState } from '../../components/EmptyState.jsx';

export function CameraPage() {
  return <EmptyState icon={Camera} label="Camera view — migrating" />;
}
