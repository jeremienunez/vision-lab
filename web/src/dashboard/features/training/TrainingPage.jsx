import { GitBranch } from 'lucide-react';
import { EmptyState } from '../../components/EmptyState.jsx';

export function TrainingPage() {
  return <EmptyState icon={GitBranch} label="Training view — migrating" />;
}
