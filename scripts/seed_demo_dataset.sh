#!/usr/bin/env sh
set -eu

printf '%s\n' "Seed dataset command placeholder."
printf '%s\n' "Expected future flow: create desk-objects-v1, upload fixtures, add annotations, and create dataset version v1."
printf '%s\n' "This script becomes executable once the Rust API and Docker Compose stack exist."
